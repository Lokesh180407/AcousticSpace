# ML Pipeline — AcousticSpace

This folder contains the data curation and Librosa feature-extraction pipeline for Week 1.

## Structure
- `data/raw/` — raw datasets (ASVspoof, SLR28 RIRs) — gitignored, not committed
- `data/protocols/` — dataset label/protocol files and curated index CSVs
- `src/` — pipeline scripts (dataset curation, feature extraction, RIR analysis)
- `notebooks/` — exploration notebooks

## Status
- [x] Folder structure created
- [ ] ASVspoof dataset curated
- [ ] RIR dataset curated
- [ ] Librosa feature extraction pipeline built